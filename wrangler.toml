name = "layerledger-bot"
main = "src/index.js"
compatibility_date = "2026-08-17"

[triggers]
crons = ["15 7 * * *"] # TEMP: test run, revert to ["0 7 * * 1-5"] after

# Stores in-progress /new and /status wizard state per chat.
# Create with `wrangler kv namespace create DRAFTS` (or via the dashboard:
# Workers & Pages -> KV) and paste the resulting id below before deploying.
[[kv_namespaces]]
binding = "DRAFTS"
id = "47568d9792414495adb62cf0646d3b49"
